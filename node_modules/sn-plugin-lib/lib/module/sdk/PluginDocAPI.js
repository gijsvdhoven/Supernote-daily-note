"use strict";

import NativePluginAPI from '../module/NativePluginAPI';
import APIError from "../error/APIError.js";
import { verifyParams } from "./utils/VerifyUtils.js";
export default class PluginDocAPI {
  /**
   * Gets the selected text.
   * @returns {Promise<APIResponse<string>>} Returns:
   * {
   *  success: boolean  // Whether the API call succeeded
   *  result: string   // Selected text from the current file
   *  error: { // Present only when success is false
   *    code: number  // Error code
   *    message: string  // Error message
   *  }
   * }
   */
  static async getLastSelectedText() {
    return NativePluginAPI.getLastSelectedText();
  }

  /**
   * Gets the text content of a page in the currently opened document.
   * @param {number} page Page index
   * @returns {Promise<APIResponse<string>>} Returns:
   * {
   *  success: boolean  // Whether the API call succeeded
   *  result: string   // Text content of the specified document page
   *  error: { // Present only when success is false
   *    code: number  // Error code
   *    message: string  // Error message
   *  }
   * }
   */
  static async getCurrentDocText(page) {
    try {
      verifyParams({
        page: {
          type: 'number',
          required: true,
          integer: true,
          min: 0
        }
      }, {
        page
      }, {
        allowUnknown: false,
        rootName: 'getCurrentDocText'
      });
    } catch (error) {
      if (APIError.isAPIError(error)) {
        return {
          success: false,
          error: {
            code: error.code,
            message: error.message
          }
        };
      } else {
        return {
          success: false,
          error: {
            code: 100,
            message: error.message
          }
        };
      }
    }
    return NativePluginAPI.getCurrentDocText(page);
  }

  /**
  * Gets total page count of the currently opened document.
  * @returns {Promise<APIResponse<number>>} Returns:
  * {
  *  success: boolean  // Whether the API call succeeded
  *  result: number   // Total page count of the document
  *  error: { // Present only when success is false
  *    code: number  // Error code
  *    message: string  // Error message
  *  }
  * }
  */
  static async getCurrentTotalPages() {
    return await NativePluginAPI.getCurrentDocTotalPages();
  }

  /**
  * 生成文档图片.
  * @param {string} docPath Document path
  * @param {number} page Page index
  * @param {string} pngPath Output image path
  * @param {object} size Image size:
  * {
  *  width: number  // Image width
  *  height: number  // Image height
  * }
  * @returns {Promise<APIResponse<boolean>>} Returns:
  * {
  *  success: boolean  // Whether the API call succeeded
  *  result: boolean   // Whether the image generation was successful
  *  error: { // Present only when success is false
  *    code: number  // Error code
  *    message: string  // Error message
  *  }
  * }
  */
  static async generateDocImage(docPath, page, pngPath, size) {
    try {
      verifyParams({
        docPath: {
          type: 'string',
          required: true
        },
        page: {
          type: 'number',
          required: true,
          integer: true,
          min: 0
        },
        pngPath: {
          type: 'string',
          required: true
        },
        size: {
          type: 'object',
          required: true,
          properties: {
            width: {
              type: 'number',
              required: true,
              min: 1
            },
            height: {
              type: 'number',
              required: true,
              min: 1
            }
          }
        }
      }, {
        docPath,
        page,
        pngPath,
        size
      }, {
        allowUnknown: false,
        rootName: 'generateDocImage'
      });
    } catch (error) {
      if (APIError.isAPIError(error)) {
        return {
          success: false,
          error: {
            code: error.code,
            message: error.message
          }
        };
      } else {
        return {
          success: false,
          error: {
            code: 100,
            message: error.message
          }
        };
      }
    }
    return NativePluginAPI.generateDocImage(docPath, page, pngPath, size);
  }
}
//# sourceMappingURL=PluginDocAPI.js.map